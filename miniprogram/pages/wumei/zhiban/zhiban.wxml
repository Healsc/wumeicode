<view class="detail">
	<block wx:for="{{list}}" wx:key="index">
		<view class="content">
			<view class="content-week">{{item._week}}</view>
			<wxparser rich-text="{{item._content}}" />
           
		</view>
	</block>
</view>
